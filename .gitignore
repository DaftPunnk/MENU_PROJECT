# Dependencies / 依赖
node_modules/
/.pnp
.pnp.js

# Production builds / 生产构建
dist/
build/

# Environment files / 环境变量
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor / 编辑器
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs / 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Claude Code / Claude Code 配置
.claude/
CLAUDE.md

# Notes (don't push) / 个人笔记不上传
NOTES.md

# Python cache / Python 缓存
__pycache__/

# 原始菜品照片(图像流水线的输入,不被 app 使用) / raw dish photos (pipeline inputs, not used by the app)
src/assets/*.jpg
